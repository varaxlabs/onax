apiVersion: v1
kind: Service
metadata:
  name: {{ include "varax-monitor.fullname" . }}
  labels:
    {{- include "varax-monitor.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "varax-monitor.selectorLabels" . | nindent 4 }}

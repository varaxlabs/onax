apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "varax-monitor.fullname" . }}
  labels:
    {{- include "varax-monitor.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "varax-monitor.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "varax-monitor.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
